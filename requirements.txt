# Dependencies để CHẠY project host demo này (module + voice + dev).
# Khi cài module vào project khách, dùng: pip install "poppy-assistant[voice,phone]"
# (deps được khai trong pyproject.toml — đây chỉ là bản tiện cho repo demo).

# --- Lõi (chat text + RAG) ---
Django>=5.0,<6.1
channels>=4.1.0
daphne>=4.1.0
openai>=1.30.0
chromadb>=0.5.0
python-frontmatter>=1.1.0
requests>=2.31.0

# --- Voice (gọi online + gọi số điện thoại) ---
google-genai>=1.0.0

# --- Tiện dev ---
python-dotenv>=1.0.0
